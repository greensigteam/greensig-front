# Backend API Configuration
# URL de base de l'API Django (sans le trailing slash)
VITE_API_BASE_URL=http://127.0.0.1:8000/api

# Frontend Port (Vite dev server)
# Décommenter pour changer le port par défaut (3000)
# VITE_PORT=3000

# Optional: API timeout in milliseconds
# VITE_API_TIMEOUT=30000

# Novu Notifications (temps réel)
# Récupérez votre Application Identifier sur https://web.novu.co
VITE_NOVU_APP_ID=your-novu-application-identifier

# Instructions:
# 1. Copier ce fichier en .env
# 2. Ajuster VITE_API_BASE_URL si le backend est sur un autre port/host
# 3. Ne jamais committer le fichier .env (déjà dans .gitignore)
